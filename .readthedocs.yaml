# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details
# Required
---
version: 2
# Set the OS, Python version, and other tools you might need
build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  jobs:
    post_checkout:
      - git fetch origin main || true
      - git log --oneline -n 5
      - git diff --name-only FETCH_HEAD
      - git fetch origin main || true
      - |
        if git diff --quiet FETCH_HEAD -- README.md; then
          echo "No changes in README.md, exiting with 183"
          exit 183
        else
          echo "README.md has changes, continuing build"
        fi
    pre_build:
      - |
        python -m sphinx -b linkcheck -D linkcheck_timeout=1 README.md \
        $READTHEDOCS_OUTPUT/linkcheck
    pre_install:
      # <-- This runs your script before Sphinx builds
      - python strip_toc.py
sphinx:
  configuration: conf.py
  fail_on_warning: true
formats:
  - epub
  - pdf
python:
  install:
    # Install our python packages before building the docs
    - requirements: requirements.txt
